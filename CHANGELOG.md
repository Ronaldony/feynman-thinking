# Changelog

## 0.4.0

- 스킬의 목적을 파인만의 말투·인격 모사가 아닌 **관찰 가능한 연구 행동의 모방**으로 명확히 정의함.
- `이름 벗기기 → 모델·재표현 → 직접 작업 → 모델 공격 → 수정·설명`의 5단계 핵심 추론 루프를 L1 이상 필수 동작으로 도입함.
- 템플릿이나 태그보다 실제 유도·계산·추정·자료 확인·코드 실행·테스트를 우선하는 `실행이 보고보다 우선` 규칙을 추가함.
- 실행하지 않은 검사는 `미검증`, 관련 없는 검사는 `해당 없음(N/A)`으로 구분하도록 수정함.
- 불리한 증거가 결론·모델·범위·확신을 실제로 바꾸지 않으면 과정 실패로 보는 표면 모방 방지 감사를 추가함.
- 파인만 특이 지표와 일반 비판적 사고 대조군을 포함한 행동 평가 프로토콜을 재설계함.
- 12개의 도메인별 추론 사례와 0/1/2/N/A 채점 루브릭, 하드 게이트를 추가함.
- 외부 에이전트 반복 실행용 `scripts/run_eval.py`와 조건 블라인드 검토 패킷 생성기 `scripts/prepare_blind_review.py`를 추가함.
- 구조 검증기가 평가 자산, Python 문법과 루트/패키지 미러의 바이트 단위 일치를 확인하도록 확장함.
- GitHub Actions에서 구조 검증, 스크립트 컴파일과 평가 프롬프트 조립을 자동 검사하도록 함.

## 0.3.0

- Open Agent Skills 형식에 맞춰 폴더명과 frontmatter `name`을 일치시킴.
- 임의의 최상위 `version`, `language` 필드를 `metadata` 아래로 이동함.
- 긴 근거와 절차를 `references/`로 분리해 누진적 로딩 구조를 적용함.
- 재사용 작업 문서를 `assets/`로 분리함.
- `agents/openai.yaml`을 추가해 표시 이름과 암묵 호출 정책을 명시함.
- 발동 평가 사례와 로컬 검증 스크립트를 추가함.
- 반증 편향을 주장 유형별 검증으로 교정하고, 사회·정책 도메인 가드레일을 강화함.
