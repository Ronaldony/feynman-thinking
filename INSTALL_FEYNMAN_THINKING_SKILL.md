# 설치 안내

이 패키지는 저장소 루트에서 압축을 풀면 다음 위치에 설치된다.

```text
.agents/skills/feynman-thinking/
```

이미 `.agents` 폴더가 있어도 동일한 구조로 병합하면 된다. 설치 후 다음 명령으로 점검한다.

```bash
python .agents/skills/feynman-thinking/scripts/validate_skill.py
```

Codex가 변경을 자동 인식하지 않으면 Codex를 다시 시작한다.
